# Sending Gmail account (From)
STUDIO_EMAIL=
# Instructor notification inbox
INSTRUCTOR_EMAIL=
# Gmail SMTP — App Password for SMTP_USER
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=

# Optional: override booking store path (default: ./data/bookings.json)
# DB_PATH=/app/data/bookings.json
