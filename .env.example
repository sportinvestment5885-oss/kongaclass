# Sending Gmail account (From) — must match SMTP_USER
STUDIO_EMAIL=szpitor@gmail.com

# Instructor notification inbox
INSTRUCTOR_EMAIL=konkolykitty@gmail.com

# Gmail SMTP — 16-char App Password for SMTP_USER (spaces ok, we strip them)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=szpitor@gmail.com
SMTP_PASS=

# Accepted aliases: SMTP_PASSWORD, GMAIL_APP_PASSWORD, GMAIL_USER, NOTIFY_EMAIL

# Booking store
# DB_PATH=/app/data/bookings.json
