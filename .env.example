# Studio inbox + From address
STUDIO_EMAIL=szpitor@gmail.com

# Gmail SMTP — use an App Password (Google Account → Security → App passwords)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=szpitor@gmail.com
SMTP_PASS=

# Optional: override SQLite path (default: ./data/bookings.db)
# DB_PATH=/data/bookings.db
